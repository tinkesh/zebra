<% content_for :breadcrumb do %>
  <ul id="breadcrumb">
    <li><%= link_to 'Dashboard', private_home_path %></li>
    <li>»</li>
    <li><%= @page_title %></li>
  </ul>
<% end %>

<<<<<<< HEAD
<hr>
<table>
<% form_for @search do |f| %>
  <tr class="no-border"><td class="no-border right">
		<%= f.text_field :job_name_like %>
		<%= f.submit "Search" %><br>
		<em>Searches within job</em>
  </td></tr>
<% end %>
=======
<table>
  <% form_for @search do |f| %>
    <tr class="no-border">
      <td>
        <%= f.label :job_name_like, "Job" %><br />
        <%= f.text_field :job_name_like %>
      </td>
      <td>
        <%= link_to "Clear Search", private_job_sheets_path %><br/>
        <%= f.submit "Search" %>
      </td>
    </tr>
  <% end %>
>>>>>>> 47997ff1564b43ec7da48d71469d64fa72e6a29d
</table>

<%= will_paginate @job_sheets %>

<table class="sortable">
  <tr>
    <th class="center number">#</th>
    <th class="span-2 center">Date</th>
    <th class="number">Job</th>
    <th class="actions nosort"></th>
  </tr>

<% @job_sheets.each do |job_sheet| %>
  <tr>
    <td><%= job_sheet.id %></td>
    <td><%= if (job_sheet.gun_sheets && job_sheet.gun_sheets.first && job_sheet.gun_sheets.first.started_on) then job_sheet.gun_sheets.first.started_on.strftime('%b-%d-%y') end %></td>
    <td>
      <%= job_label(job_sheet.job) %>
      <%= link_to image_tag("open-link.png"), private_job_path(job_sheet.job) %>
    </td>
    <td class="actions">
    <%= link_to 'Delete', private_job_sheet_path(job_sheet), :confirm => 'Are you sure? This cannot be undone!', :method => :delete, :class => "delete" %>
    <%= link_to "Edit", edit_private_job_sheet_path(job_sheet), :class => "edit" %>
    <%= link_to "Show", private_job_sheet_path(job_sheet), :class => "show" %>
    </td>
  </tr>
<% end %>
</table>

<%= will_paginate @job_sheets %>
